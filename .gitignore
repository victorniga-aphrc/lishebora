# --- Python / FastAPI project ---
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
htmlcov/
.coverage*
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
venv/
.venv/
env/
.envrc
.python-version

# IDE / editor settings
.vscode/
.idea/
*.swp
*.swo

# OS-specific
.DS_Store
Thumbs.db

# Environment / secrets
.env
.env.*
!.env.example
*.key
*.pem
start.sh
stop.sh
*.zip

# AWS credentials and keys (never commit)
aws/

# Large local DB dumps (do not commit; slows git add and bloats the repo)
supermarket_a.backup

# Notebooks (optional – remove if you want them in git)
*.ipynb_checkpoints/

# Machine-local / scratch (shell helpers, notes, optional tests, archived markdown — not versioned)
local/

# Optional project documentation at repo root (keep under local/ if you want it on disk)
docs/

# Root-level tests (optional automation lives under local/tests/)
tests/

# Research / bulky assets (keep locally; not part of the runtime API tree)
concept/
ingredient_image_data/
notebooks/

# Runtime / editor noise
logs/
.run/

# Large raw ETL inputs (obtain separately; see data/README.md)
data/huge_data.csv
data/all_categories_combined.csv

